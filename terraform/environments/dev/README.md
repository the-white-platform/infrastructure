# This file creates a symbolic link to the main Terraform configuration

# Run this to set up the environment:

# ln -sf ../../main.tf main.tf

# ln -sf ../../variables.tf variables.tf

# ln -sf ../../outputs.tf outputs.tf

# ln -sf ../../versions.tf versions.tf

# Or run: bash setup-env.sh
