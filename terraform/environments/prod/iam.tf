# IAM permissions for cross-project access
# Note: The actual cross-project IAM binding (prod SA reading from dev Artifact Registry)
# is managed in dev/iam.tf, not here, to avoid cross-project permission issues.
# This file is reserved for future cross-project permissions if needed.
